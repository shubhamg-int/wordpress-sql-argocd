apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
  app: wordpress
spec:
  # type: ClusterIP
  selector:
    app: wordpress
    tier: frontend
  type: LoadBalancer
  ports:
  - port: 80
    name: wordpress